# stata-outline
This VS Code extension adds **Outline support for Stata `.do` files**.   It automatically recognizes comment lines with multiple `%` symbols as section headers, supporting hierarchical structure in the Outline view.
